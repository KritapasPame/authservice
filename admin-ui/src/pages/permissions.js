// admin-ui/src/pages/permissions.js — หน้า 4: ตั้งสิทธิ์ราย user (groupcompanyadmin/admin)
// STUB (T1) — จะถูกแทนที่ทั้งไฟล์โดย T4
import { route } from '../router.js'

route('#/permissions', (view) => {
  view.innerHTML = `
    <div class="frame">
      <div class="framebar">🏢 จัดการผู้ใช้</div>
      <div class="framebody">
        <div class="placeholder">กำลังพัฒนา</div>
      </div>
    </div>
  `
})
