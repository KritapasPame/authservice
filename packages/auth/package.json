{
  "name": "@platform/auth",
  "version": "1.2.0",
  "description": "Shared JWT auth middleware + permission helpers for platform services (entitlement, esign, ...)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && bun build ./src/index.ts --outdir dist --target node --format esm --packages external && tsc -p tsconfig.json"
  },
  "dependencies": {
    "jose": "^6.2.3"
  },
  "peerDependencies": {
    "elysia": ">=1.4.0"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
